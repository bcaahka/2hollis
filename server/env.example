# Copy to server/.env if you want to override defaults.
PORT=8787
HOST=0.0.0.0
ADMIN_KEY=dev

# In the app root, optional:
# VITE_API_URL=http://localhost:8787
# Leave empty during web dev — Vite proxies /api to this server.

